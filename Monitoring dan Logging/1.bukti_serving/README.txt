Letakkan screenshot bukti model SEDANG di-serve, misalnya:
- Terminal `docker run -p 8080:8080 epamemo/telco-churn-ci:latest` yang berjalan, ATAU
- `mlflow models serve ...` aktif, ATAU
- Hasil curl/response dari endpoint /invocations (200 OK).

Contoh nama file: serving_docker.jpg
