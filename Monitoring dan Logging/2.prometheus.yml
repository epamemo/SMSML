global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "telco_churn_exporter"
    static_configs:
      - targets: ["localhost:8000"]
        labels:
          service: "model-serving"
          owner: "epamemo"

  # Opsional: scrape endpoint MLflow serving langsung bila mengekspos metrik.
  # - job_name: "prometheus"
  #   static_configs:
  #     - targets: ["localhost:9090"]
